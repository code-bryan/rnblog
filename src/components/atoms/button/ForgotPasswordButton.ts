import styled from 'styled-components/native';
import { Button, NativeBase } from 'native-base';

const ForgotPasswordButton: React.FC<NativeBase.Button> = styled(Button)`
  width: 50%;
  align-self: flex-end;
`;

export default ForgotPasswordButton;
