import Authentication from '../../models/Authentication';

const INITIAL_STATE: any = {
};

const Reducer = (state: any = INITIAL_STATE) => state;

export const authenticateUser = (authentication: Authentication) => {
  return
};

export default {
  Reducer,
};
