import { ActionReducer } from '../../types/ActionReducer';

const INITIAL_STATE: any = {};
const Reducer = (state: any = INITIAL_STATE, action: Partial<ActionReducer>) => state;

export default {
  Reducer,
};
